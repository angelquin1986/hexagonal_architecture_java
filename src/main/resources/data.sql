CREATE TABLE country (
                         id   INTEGER      NOT NULL AUTO_INCREMENT,
                         rappiId   INTEGER      NOT NULL AUTO_INCREMENT,
                         nameRt VARCHAR(128) NOT NULL,
                         status INTEGER NOT NULL,
                         PRIMARY KEY (id)
);